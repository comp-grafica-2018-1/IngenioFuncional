<p id="notice"><%= notice %></p>

<h1>Cotiz Mes Exps</h1>

<table>
  <thead>
    <tr>
      <th>Correo</th>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cotiz_mes_exps.each do |cotiz_mes_exp| %>
      <tr>
        <td><%= cotiz_mes_exp.correo %></td>
        <td><%= cotiz_mes_exp.nombre %></td>
        <td><%= link_to 'Show', cotiz_mes_exp %></td>
        <td><%= link_to 'Edit', edit_cotiz_mes_exp_path(cotiz_mes_exp) %></td>
        <td><%= link_to 'Destroy', cotiz_mes_exp, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Cotiz Mes Exp', new_cotiz_mes_exp_path %>
