<%= form_with(model: cotiz_esc_cam, local: true) do |form| %>
  <% if cotiz_esc_cam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cotiz_esc_cam.errors.count, "error") %> prohibited this cotiz_esc_cam from being saved:</h2>

      <ul>
      <% cotiz_esc_cam.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :correo %>
    <%= form.text_field :correo, id: :cotiz_esc_cam_correo %>
  </div>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :cotiz_esc_cam_nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
